Django forms validate user input.

ModelForm generates forms from Django models.

Forms protect against invalid data.

clean() allows custom validation.

is_valid() returns True when validation succeeds.